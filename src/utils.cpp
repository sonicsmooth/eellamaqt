#include "utils.h"
#include <string>
#include <filesystem>
#include <iostream>

void Utils::ensurePathExists(std::string fullpath) {
//    // fullpath is the name of a file or directory.  A folder
//    // is specified with a trailing slash.
//    // Create directories as necessary to ensure that
//    // fullpath (minus filename) exists
//    std::filesystem::path p(fullpath);
//    std::cerr << "current path:   " << std::filesystem::current_path() << std::endl;
//    std::cerr << "root name:      " << p.root_name() << std::endl;
//    std::cerr << "root directory: " << p.root_directory() << std::endl;
//    std::cerr << "relative path:  " << p.relative_path() << std::endl;
//    std::cerr << "parent path:    " << p.parent_path() << std::endl;
//    std::cerr << "filename:       " << p.filename() << std::endl;
//    std::cerr << "stem:           " << p.stem() << std::endl;
//    std::cerr << "extension:      " << p.extension() << std::endl;

//    for (auto const & seg : p) {
//        if (std::filesystem::exists(seg)) {
//            continue;
//        }
//        else {
//            //std::filesystem::create_directory()
//        }

//    }



}
